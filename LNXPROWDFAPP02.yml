- name:  LNXPROWDFAPP02 
  hosts: all
  tasks:
    - name: Comando 1
      shell: sudo echo "install cramfs /bin/true" >> /etc/modprobe.d/CIS.conf | rmmod cramfs
    - name: Comando 2
      shell: sudo echo "install freevxfs /bin/true" >> /etc/modprobe.d/CIS.conf | rmmod freevxfs
      
